#!/bin/bash
set -e -x

source $(dirname $0)/version

cd $(dirname $0)/..

IMAGE=${REPO}/system-agent-installer-rke2:${VERSION}-${ARCH}
DOCKERFILE=package/Dockerfile

docker build -f ${DOCKERFILE} -t ${IMAGE} .
echo Built ${IMAGE}
